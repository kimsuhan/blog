---
interface Props {
  links?: Array<{
    slug: string;
    title: string;
    description: string | null;
  }>;
}

const { links = [] } = Astro.props;
---

{links.length > 0 && (
  <aside class="post-links-section" aria-labelledby="backlinks-heading">
    <p class="archive-label archive-muted">Backlinks</p>
    <h2 id="backlinks-heading">Referenced by</h2>
    <ul>
      {links.map((link) => (
        <li>
          <a href={`/posts/${link.slug}`}>{link.title}</a>
          {link.description && <p>{link.description}</p>}
        </li>
      ))}
    </ul>
  </aside>
)}
